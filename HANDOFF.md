# HANDOFF — 변호사시험 사례형 학습 시스템

작성일: 2026-07-27 · **최종 갱신: 2026-08-08**
원본 세션: `f172b145-1486-46f2-beaf-564463f95d74` (2026-07-24 ~ 07-27, 세션 한도로 여러 번 중단되며 진행)
이 문서는 그 세션이 마지막 응답 도중 세션 한도로 끊겨서, 다음 세션에서 복원용으로 작성함.

> **아래 "1부"는 2026-07-27 시점의 기록이다.** 그 뒤 07-28 ~ 08-07에 상당한 변화가 있었으므로
> **먼저 이 절을 읽을 것.** 1부 본문 중 뒤집힌 서술에는 취소선을 그어 두었다.

---

## 2026-08-08 갱신 — 07-27 이후 무슨 일이 있었나

### 한 줄 요약

AI 채점이 **켜졌고**, 사례집이 **9권으로 늘었고**(재OCR로 되살린 책 포함), 세 과목 모두 **목차 모드가 생겼고**,
민사법에 **창작문제 163건**(정연석 로사정)이 들어와 55건 → **218건**이 됐다.
그리고 앱이 노션 「로민정 단원」 DB와 **딥링크로 이어졌다.**

### 1) AI 채점 — 구현·배포 완료 (커밋 `fc77ad5`, 2026-07-29)

미해결 이슈 8번·다음에 할 일 9번이 **해결됐다.** Cloudflare Worker 프록시 소스는 저장소 `worker/`에
있고, 배포 주소는 `index.html`의 `GRADER_URL` 상수에 박혀 있다. 재배포는 `worker/`에서 `wrangler deploy`.
채점 근거(채점기준표 또는 사례집 모범답안)가 없는 회차에서는 버튼이 동작하지 않는다.

### 2) 사례집 재OCR — 이게 8/5 작업의 대부분이었다

"확보한 사례집 텍스트가 깨져 있다"는 게 드러나 여러 권을 **다시 OCR해서 갈아끼웠다.**
커밋 메시지에 개선 폭이 남아 있다 — 민사법 깨짐 **55% → 1%**, 형사법 표기 깨짐 **51% → 2%**,
인사이트 상법 **24% → 9%**, 송영곤 민소사연 **9% → 0%**.

**교훈: "텍스트가 추출됐다"와 "텍스트가 맞다"는 다른 문제다.** 추출 성공률만 보고 넘어가면
사례집이 통째로 못 쓰는 상태인 걸 모른 채 앱에 넣게 된다. 새 책을 넣을 때는 깨짐률을 반드시 잰다.

책도 늘었다 — 이인규 형법 변사기, 작은변사기 형사소송법, 인사이트 상법, 송영곤 민소사연,
박승수 CBT실전답안, 정선균 행정법 기출해설. 지금 9권이고 과목별 내역은 README 표에 있다.
민사법 모범답안은 25회차 → 53회차, 형사법은 10회차 → 15회차로 늘었다(커밋 `94209cd`, `af85cbf`).

### 3) 형사법·민사법 목차 모드 — 만들었다 (미해결 이슈 13번 해소)

"책 구조가 안 맞아 보류"했던 것을, 주제별 목차를 가진 책들을 추가로 넣어 해결했다.
**세 과목 모두 `casebook_toc.json`이 있다.** 창작 사례가 많은 책(이창현·작은변사기)은
실제 시험과 매칭 안 되는 항목이 "미연결"로 남는다 — 이건 정상 동작이다.

### 4) 민사법 창작문제 163건 — 정연석 「로사정」 (커밋 `4c7f051`~`06a26cb`)

기출이 아닌 **창작문제**를 처음으로 넣었다. `examType`이 `실제기출`·`모의고사`에 더해
`창작문제` 세 번째 값을 갖게 됐고, 목록 상단 `구분` 필터로 나눠 볼 수 있다.
기출과 같은 스키마라 문제 풀기·모범답안·AI 채점이 그대로 동작한다.

전용 파이프라인 4개(`extract_rosajeong.py` → `reflow_rosajeong.py` → `parse_rosajeong.py` →
`build_rosajeong_민사법.py`)는 README "로사정 파이프라인" 절에 설명해 뒀다. 되돌릴 수 있게
만들어져 있다 — 몇 번을 다시 돌려도 기존 55건은 두고 `민사법_로사정_*`만 걷어낸 뒤 새로 붙인다.

**이 작업에서 가장 중요한 것은 OCR 교정이다.** 원본 PDF에 당사자 기호 `乙`이 **0회** 등장했다
(`甲`은 2,073회). 전량 `江`·`己`로 오인식된 것이다. 사례집에서 甲乙丙이 뒤바뀌면 답안이 무의미해지므로
이건 치명적이다. 잔여 의심 자리 395건을 전부 지면 이미지와 1:1 대조해 교정했고,
검증 결과는 `pipeline/casebook/rosajeong_verify.txt`에 **0건**으로 남아 있다 —
**다시 0이 아니게 되면 새 문제가 생긴 것이다.**

대조는 쪽을 통째로 렌더링하지 말고 **의심 자리의 그 줄만 잘라 세로로 쌓아야** 한다
(`make_verify_sheets.py`). 쪽 단위면 한 건에 한 장이지만 줄 단위면 한 장에 26건이 담긴다.
판정은 반드시 **교정 후 낱말**로 한다 — 원문 낱말로 하면 이미 해결된 자리까지 다시 걸려
확인할 자리가 360건에서 4,333건으로 부푼다.

한글끼리의 오인식(예: `그!` ← `각`)은 **자동으로 못 찾는다.** 편집거리 1 후보는 한국어가 교착어라
정상 활용형끼리도 걸려 5,185건이 나온다 — 쓸 수 없다. **희귀 음절 빈도**가 그나마 가장 잘 걸린다
(46만 음절 중 3회 이하인 156종만 훑으면 대부분 드러남). 남은 것을 다 잡으려면 632쪽을 사람이 읽는 수밖에 없다.

조판 줄바꿈 되돌리기(`reflow_rosajeong.py`)에서 알아둘 것: 여백은 **쪽이 아니라 블록마다** 재야 한다.
문제 지문은 음영 박스 안이라 본문보다 10pt쯤 좁아서, 쪽 전체 최댓값으로 재면 지문만 재조립이 안 된다.
붙일지 띄울지 애매하면 **줄바꿈을 그대로 둔다** — 덜 이어붙는 대신 없던 오류를 만들지 않는다.

각주 파싱이 함정이었다. 각주 번호는 **PART마다 1로 리셋**되고, 쪽 경계를 무시하고 줄 단위로만
판단하면 다음 쪽 본문이 통째로 각주에 딸려 들어가 답안이 잘린다(7개 사례가 그랬다).
`_block_start()`가 맨 끝 각주에서 위로 거슬러 올라가며 번호가 1씩 줄어드는 구간을 찾는 이유다.

### 5) 앱 ↔ 노션 딥링크 (커밋 `84ef89f`, `e74e79c`)

`?exam=<시험id>`를 추가했다(기존 `?issue=`와 같은 방식). 과목은 id 앞부분에서 읽으므로
마지막에 보던 과목이 무엇이든 링크 하나로 정확히 열린다. id에 한글이 들어가지만 URL에 그대로 써도 된다.

이 링크로 노션 「로민정 단원」 DB의 CASE 섹션 **61단원에 183줄**을 걸었다.
**대응표와 그 근거는 노션이 아니라 `pipeline/scripts/link_romin_case.py`의 `MAP`에 있다** —
노션은 판본 관리가 안 되기 때문이다. 강의 전사에 사례 번호 근거가 없어(강사가 번호를 부른 건 3번뿐)
번호가 아니라 **논점**으로 이었다. 가족법 22단원 + 대응 사례 없는 11단원은 비워 뒀다.

### 6) 반드시 알아야 할 함정 두 가지

**⚠️ 커밋만 하면 반영되지 않는다.** 이 앱은 GitHub Pages 배포라 **푸시까지 해야** 사이트가 바뀐다.
07-27 기록에는 "커밋 완료"로 끝나는 항목이 많은데, 그 상태로는 사이트에 아무 일도 일어나지 않는다.

**⚠️ `CASE_Practice` 클론이 두 개다.** 정본은 `C:\Users\82109\CASE_Practice`이고,
`Desktop\2026\projects\CASE_Practice`는 2026-07-27에서 멈춘 구버전이다.
`.claude/launch.json`이 구버전을 가리키고 있어 프리뷰가 옛 데이터를 보여주는 사고가 있었다 — 정본으로 고쳤다.

### 7) Pages 빌드 실패를 두 번 오진했다 (기록용)

로사정 163개 파일을 한꺼번에 올린 뒤 Pages 빌드가 실패했다. **사이트 용량 탓이라 짐작하고**
`.nojekyll`을 넣었다가(효과 없어 되돌림) `_config.yml`로 `pipeline`·`worker`·`docs`를 제외했다.
둘 다 오진이었다. check-runs annotations API(`/repos/.../check-runs/{id}/annotations`)를 보니
실제 원인은 `"The job was not acquired by Runner of type hosted even after multiple attempts"` —
**GitHub 러너 할당 실패**로, 내용과 무관했다. 그냥 다시 푸시하니 통과했다(`3756c06`).
실패 소요시간 452s/900s/902s는 빌드 시간이 아니라 **큐 대기 시간**이었다.

`_config.yml`은 실제로 사이트 용량을 줄여주므로 그대로 뒀다(파일에 그 경위를 주석으로 남김).
**교훈: Actions 실패는 로그 요약이 아니라 annotations API까지 봐야 원인이 나온다.**

### 8) 07-27 기록 중 지금은 틀린 것

| 1부의 서술 | 지금 |
|---|---|
| 미해결 이슈 8 — AI 채점 미구현 | ✅ 배포됨 (`worker/`) |
| 미해결 이슈 13 — 형사법 목차 모드 없음 | ✅ 만들었음 (3권) |
| 다음에 할 일 13 — 형사법·민사법 목차 모드 보류 | ✅ 둘 다 완료 |
| "형사법 사례집 모범답안 10회분" | 15회분 |
| "민사법 55건 / index 451KB / exams 6.3MB" | 218건 / 779KB / 7MB |
| "형사법·민사법은 `casebook_toc.json`이 없음" | 세 과목 모두 있음 |

### 9) 지금 남아 있는 것

1. **문항 단위 딥링크(`&group=`)** — 앱 내부에 `focusGroupKey` 상태는 이미 있는데 URL로 노출되어 있지 않다.
   기출(변시 15 + 모의 40)을 노션 단원별로 링크하려면 이게 먼저다. 기출은 한 시험에 논점이 여럿 섞여 있어
   `?exam=`만으로는 시험 전체가 열려 제값을 못 한다. **로사정이 안 다루는 가족법 22단원은 기출로만 채울 수 있다.**
2. **국제법·국제거래법** — 태깅 데이터는 `pipeline/source/`에 있고 앱 반영 전. 1부 "다음에 할 일" 4번 그대로.
3. 1부 미해결 이슈 중 **9·12·20**(쟁점 자동분류 실패율), **18**(민사법 변시 7건 채점근거 공백)은 그대로 남아 있다. 문항 라벨(**16·22·23**)은 2026-08-11 원본 시험지에서 표제를 읽어 민사법 55건 전부 확정했다.

---

## 1부 — 2026-07-27 시점의 기록

---

## 배경

변호사시험(변시) 사례형 대비를 위해 두 갈래 작업을 진행 중:
1. 로컬 hwp/pdf 기출자료 정리 → 텍스트 추출 → 쟁점 태깅 → 출제경향 분석
2. 그 데이터로 사례형 답안작성·채점·아카이빙 웹앱(`CASE_Practice`) 구축

전체 학습 시스템은 허브(`evenoa9218-gif.github.io`) + 위성앱(선택형/사례형/암기장/기록형) 구조로 설계됨.

---

## 확정된 결정사항

**파일 분류 규칙**
- 6개 과목 분류: 공법 / 국제거래법 / 국제법 / 기록형 / 민사법 / 형사법 → 이후 기록형은 독립 폴더가 아니라 공법·민사법·형사법 각 과목 산하 `문제/기록형`, `채점기준표/기록형` 하위폴더로 재배치(국제법·국제거래법은 선택과목이라 기록형 시험 자체가 없음)
- 각 과목 `문제`/`채점기준표` 밑에 `선택형`/`사례형`(+해당 시 `기록형`) 하위폴더로 통일
- 지정한 6과목 외 선택과목(조세법·노동법·경제법·지적재산권법·환경법) 파일은 삭제
- 전 과목이 한 파일에 뭉쳐 있어 자동 분리가 불가능한 통합 파일("선택형 최종정답", "선택과목 사례형" 등)은 그대로 보관 — 사용자가 직접 처리하기로 함
- 완전 중복(바이트 동일) 파일은 자동 정리, 내용이 다른데 동일 시험으로 보이는 파일은 삭제하지 않고 `_보류항목_사례집대조필요.md`에 기록 후 사례집 확보 시 대조하기로 함

**텍스트 추출**
- hwp 파일 파싱은 한글(HWP) 프로그램 COM 자동화가 아니라 `pyhwp` 라이브러리(순수 파이썬)로 채택 — 이유는 아래 "버린 접근" 참고
- "배포용" 표시가 있는 hwp도 실제로는 DRM/암호화가 아니라 한글 프로그램 UI 제한일 뿐이라 pyhwp로 추출 가능함을 확인
- 사례집(모범답안집) PDF는 `pypdf`/텍스트층 판별 후 변환, OCR 텍스트층 없는 파일은 후순위로 미룸

**아키텍처**
- 허브: `evenoa9218-gif.github.io` (루트 저장소, PWA/서비스워커 스코프를 `/`로 전체 커버하기 위해 반드시 이 이름이어야 함) — 저장소만 생성됨, 실제 페이지 미구축
- 위성 저장소: `MCQ`(선택형, 구 이름 `3000`, 배포됨) / `CASE_Practice`(사례형, 구 이름 `BarExam_Mastery`) / `Core_Notes`(암기장, 구 이름 `LawMJ-Mastery`, 배포됨, 다른 계정이 작업 중) / 기록형(예정)
- 다른 계정과의 협업은 계정·비밀번호 공유가 아니라 **GitHub Collaborator 초대**로 해결 — 오리진을 `evenoa9218-gif.github.io` 산하로 유지해야 localStorage/IndexedDB·서비스워커 스코프가 공유되기 때문
- Firebase는 새로 만들지 않고 기존 `MCQ`가 쓰는 공개 RTDB를 재사용, 네임스페이스만 분리(`users/...` vs `case_users/...`)
- 쟁점 ID 체계: 과목별 접두사(`PUB`/`CIV`/`CRI`/`INT`/`ITL`) + 4자리 번호, **목차 위치가 아닌 불변 ID** (기본서 개정으로 무효화 방지), 표기 흔들림은 `aliases`로 통합, 계층은 별도 `path` 필드
- `store.js` 추상화 레이어의 API 표면은 고정(`Store.init/setUser/record/getRecords/getMastery/getProgress/getDraft/setDraft/clearDraft/sync`) — 내부 구현(IndexedDB→추후 Firestore)이 바뀌어도 호출부는 불변
- 답안 전문은 기기 로컬(IndexedDB)에만 저장, 원격에는 점수·시간·쟁점ID 같은 요약만 전송 (RTDB가 인증 없는 공개 상태라 전문 노출 방지 목적)
- 디자인은 기존 MCQ(민법 등 객관식) 페이지의 라이트 테마 색상 토큰을 그대로 사용(배경 `#ecf4fb` 등), 다크모드는 토글로 유지
- 변시 사례형 답안 페이지는 "제1문/제2문" 단위로 분리, 텍스트 마커가 아니라 이미 구조화된 `questionStructure` 필드 기반 그룹핑으로 자동 분할(문항별 임시저장 가능)

**데이터 정제**
- hwp 추출본: `<표>`/`<그림>` placeholder 제거, 3연속 이상 빈 줄 축약, 줄끝/연속 공백 정리
- PDF 추출본(사례집): 문장 중간 줄바꿈을 문단으로 병합, 페이지 반복 책 머리글 제거, 법조문 표기 정규화(`제 1 조`→`제1조`), OCR 오인식 교정
- **의도적으로 하지 않은 것**: 조사(은/는/이/가) 앞 공백 자동 병합 — "있는 이 사건"의 "이"가 관형사인 경우와 기계적으로 구분 불가능해서 규칙 제거함(아래 "버린 접근" 참고)

---

## 지금 하던 작업 — 정확히 어디까지 진행했는지

**[2026-07-27 세 번째 후속 작업, 완료]** "형식을 다듬고 다른 과목으로 넘어가자" → 문제 화면 서식 개선 + 형사법 전체를 공법과 동일한 파이프라인으로 앱에 반영.

**1) 문제 화면 서식 개선** (커밋 `9e417e6`)
- 지문(사실관계)은 박스, 설문(문제)은 굵게, 참조조문은 작은 글씨+점선 구분으로 시각 분리
- `splitProblemText()`가 `<문제>`/`[참조조문]` 등 표지 또는(표지 없으면) 번호매김 문단 경계로 블록을 나눔. 형사법처럼 지문보다 법조문이 먼저 나오는 문서 대비 "맨 앞 법조문 문단 선(先)추출" 로직도 추가
- 전체 55개 시험(당시 공법만) 파싱 오류 0건 확인 후 커밋

**2) 형사법 전체를 공법과 동일한 깊이로 앱에 반영** (커밋 미완료 — 이 문서 저장 직후 커밋 예정, 아래 "수정한 파일 목록"에 실제 해시 반영)
- 옛 세션 스크래치패드의 태깅 데이터(`형사법_사례_final.json`, 55건)를 소스로 사용
- 사례집 매칭: 조균석 「형사법 사례형 해설」책(형법+형사소송법 통합, 변시 문제를 그대로 다룸)의 사례 블록을 파싱해 변시 15건 중 **10건(6~15회)**에 모범답안 결합. 1~5회는 이 책이 QR코드 온라인 제공으로 전환되어 텍스트 추출 대상에서 빠져 있어 매칭 불가 — 채점근거 없음으로 남음(모의고사 40건은 전부 기존 공식 채점기준표 있음, 별도 매칭 불필요)
- 텍스트 정제: 기존 `clean_text.py`(hwp/PDF 공용) 그대로 재사용 + 조균석 책 특유의 반복 페이지머리("사례 5. [19 - 변시(8)-1] 129") 제거 규칙 추가
- 쟁점 레지스트리: `CRI` 접두사로 550개 쟁점 등록(형법/형사소송법/형사법일반 3분류, 형사법일반 227개=41%는 자동분류 실패 — 공법의 "공법일반" 48%와 비슷한 수준의 알려진 한계)
- 앱 데이터: `data/형사법/index.json` + `data/형사법/exams/*.json`(55개, 3.5MB), `data/issues_형사법.json` 생성
- **`index.html`을 공법 전용에서 다과목 지원으로 개조**: `const SUBJECT="공법"` 하드코딩을 없애고 `SUBJECTS` 목록 + `subject` 상태로 전환. 헤더에 과목 전환 셀렉트 추가(localStorage에 마지막 선택 기억). `Solve` 컴포넌트와 `submit()`의 하드코딩된 "공법" 문자열도 전부 `subject` prop으로 교체
- 형사법용 `casebook_toc.json`(사례집 목차 브라우저)은 **의도적으로 만들지 않음** — 아래 "미해결 이슈"의 사유 참고
- 브라우저 검증: 과목 전환, 회차별/쟁점별 풀기, 변시 모범답안 표시, 전체 55개 시험 `splitProblemText` 오류 0건 확인 완료

**3) 문항을 실제 시험 표기(제1문/제1문의2 등)로 재라벨링** (커밋 미완료 — 이 문서 저장 직후 커밋)
- 계기: 사용자가 공법 15회 실제 PDF(`제15회 변호사시험 공법 사례형.pdf`)를 첨부하며, 화면의 "문제1~4"가 실제 표기(〈제1문의1〉〈제1문의2〉〈제1문의3〉〈제2문〉)와 다르고 지문 박스·문제 굵게 처리가 문항 뒷부분에서 안 된다고 지적
- PDF 대조로 확인한 규칙: 사례형은 문항(제N문)당 배점이 정확히 100점, 한 문 안에 사실관계가 여럿이면 "의1,의2,…"로 나뉨(15회: 제1문의1~3=35/35/30=100, 제2문=100)
- `relabel_groups.py`(임시 스크립트)로 이 규칙을 전체 110개 시험(공법+형사법)에 적용 — 그룹 배점을 순서대로 누적하다 100점(이상)에서 문항을 닫는 방식. 105/110건은 정확히 100점에서 딱 맞아떨어짐 확인, 5건은 안 맞아 근사치로 처리(아래 미해결 이슈 참고)
- `data/*/exams/*.json`과 `data/*/index.json`의 `groups[].key`/`.label`을 새 라벨로 일괄 교체 — 페이지 탭·페이지 제목은 이제 **110개 시험 전부** 실제 표기를 보여줌
- 지문 텍스트 안에도 `〈제1문의2〉` 같은 라벨을 사례 시작 지점에 끼워 넣는 기능 추가(`injectCaseLabels`) — 다만 텍스트에서 다시 찾은 "사례 경계 수"가 문항 그룹 수와 정확히 일치할 때만 표시(안 맞으면 틀린 라벨 대신 아예 안 붙임). 이 과정에서 버그 2개 추가 발견·수정:
  - `STATUTE_LOOKS_LIKE_RE`가 문단 시작이 아니라 문단 전체에서 "제N조"를 찾아, 사실관계 문장이 우연히 법조문을 인용하면(예: "…국민체육진흥법 제20조 제3호에 대하여…") 통째로 조문으로 잘못 삼키던 것 → 문단 시작(`^`)만 검사하도록 수정
  - 시험지 끝의 "확인 : 법무부 법조인력과장" 인증 스탬프가 사례 개수 계산에 가짜 사례로 잡히던 것 → 전처리 단계에서 제거
- 현재 지문 내 라벨(`〈…〉`)까지 정확히 표시되는 시험: 공법 11/55, 형사법 22/55. 나머지는 페이지 탭 라벨은 정확하지만 지문 안 라벨은 안 보임(안전한 미표시, 오표시 아님)
- 15회 공법(사용자가 준 PDF 기준)은 페이지 탭·지문 내 라벨 모두 완벽히 일치 확인
- 전체 110개 시험 `splitProblemText` 오류 0건 재확인

**4) 문제 풀이 화면을 좌우 2단 레이아웃으로 재구성** (커밋 미완료 — 이 문서 저장 직후 커밋)
- 계기: 사용자가 실제 CBT 시험 답안작성 화면(eves) 스크린샷을 제시하며, 현재처럼 위(문제)/아래(답안) 배치는 답안 쓰면서 계속 위로 스크롤해야 해서 불편하다고 지적
- `Solve` 컴포넌트를 `.solve-grid`(2열 그리드) 안에 `.solve-col` 두 개로 재구성 — 왼쪽: 시험 정보·문항 탭(pager)·설문 목록·지문(문제 텍스트), 오른쪽: 답안 textarea·제출/모범답안/비우기 버튼·모범답안 패널
- 각 열은 `max-height:calc(100vh - 90px)` + `overflow-y:auto`로 독립 스크롤 — 답안 쓰면서 지문을 따로 오르내릴 수 있음
- 860px 미만 너비에서는 미디어쿼리로 1열로 자동 복귀(모바일 대응)
- 브라우저 검증: 1280px·1000px(2단 정상 렌더링, 문항 탭 전환 시 우측 답안이 올바른 그룹으로 전환, 임시저장 유지 확인)과 375px(1단으로 정상 복귀) 모두 확인, 콘솔 에러 없음

**5) 답안 작성칸에 줄 표시, 버튼 라벨 통일** (커밋 `a7b0160`)
- `textarea.answer-sheet`에 `line-height:28px` + `repeating-linear-gradient` 배경으로 줄 표시 추가(라이트/다크 테마 확인)
- "다음 문"→"다음 문제", "이전 문"→"이전 문제", "이 문 비우기"→"이 문제 비우기"로 통일

**6) 민사법 전체를 공법·형사법과 동일한 깊이로 앱에 반영** (커밋 미완료 — 이 문서 저장 직후 커밋)
- 사용자가 "이제 민사법 ㄱㄱ"로 다음 과목 진행을 지시(공법→형사법→민사법 순서대로 예정된 대로)
- 사례집 매칭: 정연석 「로스쿨 민사 사례형 기출문제집[해설편]」(민법+민사소송법+상법 통합, 회차별 구성) 파싱. 이 책은 신기하게도 이미 "제N문의M" 형식의 라벨을 자체적으로 쓰고 있어 우리 라벨 체계와 그대로 맞아떨어짐
  - 모의고사 회차 표기가 "06/08/10모"(월 표기)인 것을 발견 — 법전협 모의고사가 매년 6·8·10월 시행되는 관례에 따라 06→1차, 08→2차, 10→3차로 매핑(문서 서두 "2025년 시행 법전협 모의시험 3회분" 등으로 교차 확인, 단 실제 PDF로 검증하지는 못함)
  - 변시 15건 중 **8건(4,6,7,8,10,11,13,15회)**에 모범답안 결합. 나머지 7건(1,2,3,5,9,12,14회)은 이 책에 "요약"만 있고 상세 해설이 없어 매칭 불가
  - 모의고사도 25건에서 매칭(공식 채점기준표가 이미 있어 필수는 아니고 참고자료로 추가)
- 텍스트 정제: 기존 `clean_text.py` 재사용 + 정연석 책의 반복 라벨("제1문의 1제1문의 1 …") 제거 규칙 추가
- 쟁점 레지스트리: `CIV` 접두사로 480개 쟁점 등록(민법 361/민사소송법 92/상법 27 — 상법 분류 키워드가 적어 상법 항목 다수가 민법으로 흡수됐을 가능성 있음)
- 앱 데이터: `data/민사법/index.json`(451KB) + `data/민사법/exams/*.json`(55개, 6.3MB), `data/issues_민사법.json` 생성
- **문항 재라벨링(100점 규칙)을 민사법에 적용하려다 중요한 차이 발견**: 민사법은 배점 총합이 200점이 아니라 **350점**임(전 55건 공통). 100점 규칙을 그대로 적용해 근사치로 제1문~제4문 라벨을 부여했으나(예: 15회 = 제1문 100점, 제2문 105점, 제3문 115점, 제4문 30점으로 갈라짐 — 정확히 100 단위가 아님), 실제 PDF로 검증하지 못했다. 다만 원래 태깅 데이터의 `questionStructure`가 애초에 "문제 구분 없는 설문 1~21번" 같은 평평한 번호였던 것(그대로 두면 21개 낱개 페이지가 됨)보다는 훨씬 나은 상태 — 아래 미해결 이슈 참고
- `SUBJECTS`에 민사법 추가, 브라우저 검증(과목 전환, 회차별/쟁점별 풀기, 2단 레이아웃, 모범답안 표시, 사례집 목차 없음 안전 처리, 55개 시험 `splitProblemText` 오류 0건) 완료

**그 이전에 완료되어 배포된 것** (공법 과목):
- 공법 사례형 55건(변시 15회 + 모의고사 40회) 전부 채점 근거(채점기준표 또는 사례집 모범답안) 확보, 데이터 정제 완료, 쟁점 레지스트리(424개) 연결
- "쟁점별로 한 문제씩 풀기" 모드를 사례집 목차(강성민 헌법·행정법 책, 편>사례 284건, 94% 매칭)에 맞춰 재설계(커밋 `cb7c584`, `248322a`)

---

## 미해결 이슈

1. `_보류항목_사례집대조필요.md`(변시대비 자료 폴더에 위치) — 민사법 2013년 3차(사실관계 연도 2005 vs 2012 상이) / 국제거래법 2014년 1차 채점기준표(한쪽만 실질 내용 있음), 2건 모두 사례집 확보 후 대조 필요
2. "선택과목 사례형" 통합 파일 15개(국제법·국제거래법分 추출 필요) — 사용자가 "내가 처리할 테니 놔두라"고 했으나, 실제로 처리됐는지 이후 확인된 적 없음
3. "1~12회 선택형 최종정답" 등 전 과목 통합 정답 파일 — 원본 폴더에 그대로 보관 중, 분리 안 됨
4. ~~나머지 4과목 태깅 결과가 옛 세션 임시폴더에만 있어 유실 위험~~ — 2026-07-27 **완전 해결**. 1차로 `_태깅데이터_백업/` 폴더에 복사했고, 이어서 태깅 결과·사례집 블록·레지스트리·파이프라인 스크립트 전부를 저장소 `pipeline/`으로 이전해 커밋했다. 이제 임시폴더가 사라져도 무관하며, 다른 계정·PC에서도 클론만으로 `data/` 재생성이 가능하다(재현성 검증 완료)
5. ~~공법_경향분석.md 위치~~ — 위 백업 폴더에 함께 복사 완료
6. `Core_Notes`(암기장) 저장소가 `LawMJ-Mastery`에서 개명됨 — `MCQ`(구 `3000`)에서 겪은 것과 같은 절대경로 버그가 있을 가능성이 있으나 미확인
7. 허브 저장소(`evenoa9218-gif.github.io`)는 생성만 되고 실제 대시보드/공용 서비스워커/PWA 미구축
8. ~~AI 자동채점 기능 미구현~~ — 2026-07-29 **해결**. Worker 구현·배포 완료(`worker/`, 커밋 `fc77ad5`)
9. ~~공법 쟁점 424개 중 205개(48%)가 "공법일반"으로 자동분류 실패~~ — 2026-08-11 **128개(30%)로 줄임**. 「강성민 헌법/행정법 사례형 연습」 목차를 근거로 77개를 확정했다(헌법 44·행정법 33). 남은 128개는 사례집 목차에도 없는 쟁점이라 근거가 없다 — 억지로 붙이지 않았다. 공법은 헌법 사례집 **본문**이 없어 본문 근거를 못 쓴다(행정법 본문만 넣으면 헌법 쟁점까지 전부 행정법으로 쏠린다). 헌법 사례집을 확보하면 더 줄일 수 있다
10. `Desktop/2026/projects/case-practice`(소문자, git 미초기화) 폴더는 `CASE_Practice`로 대체된 구버전 프로토타입으로 보임 — 삭제 여부 미결정, 삭제 전 사용자 확인 필요
11. 형사법 변시 1~5회(2012~2016년)는 채점 근거(채점기준표도 사례집도) 전혀 없음 — 조균석 책이 이 5개 회차를 QR코드 온라인 제공으로 돌려 텍스트 추출 불가. 다른 소스(이인규 형법 변사기는 이 회차들의 형법 파트만 다룸)를 추가로 매칭하면 형법 파트만이라도 채울 수 있음
12. ~~형사법 쟁점 550개 중 227개(41%)가 "형사법일반"으로 자동분류 실패~~ — 2026-08-11 **156개(28%)로 줄임**. 목차 31개 + 전용 사례집 본문 40개 = 71개 확정(형법 44·형사소송법 27)
13. ~~형사법은 사례집 목차 브라우저(`casebook_toc.json`)가 없음~~ — 2026-08-05 **해결**. 주제별 목차를 가진 책 3권(이인규 형법 변사기·이창현 사례형사소송법·작은변사기 형사소송법)을 넣어 만들었다. 창작 사례가 많은 두 권은 실제 시험과 매칭 안 되는 항목이 "미연결"로 남는데, 이건 정상 동작이다. 민사법도 같은 방식으로 4권(로사정 포함) 구축 완료
14. ~~`splitProblemText()`의 "법조문 선추출" 로직이 부칙 문단을 지문 블록으로 잘못 분류~~ — 커밋 전 확인 중 발견해 즉시 수정함(`STATUTE_PARA_RE`에 `^부칙\s*[<〈]` 패턴 추가), 110개 시험(공법+형사법) 재검증 통과
15. ~~형사법 문제 원문에 "확인 : 법무부 법조인력과장" 같은 이물질이 섞여 있음(추출 아티팩트로 추정)~~ — **정정**: 아티팩트가 아니라 실제 시험지 마지막 장에 있는 공식 인증 스탬프임을 사용자가 제공한 15회 공법 PDF로 확인함(형사법만이 아니라 공법 포함 모든 시험지에 공통). `splitProblemText()`에서 사례 경계 계산 노이즈가 되지 않도록 전처리 단계에서 제거하도록 수정(`CERT_STAMP_RE`)
16. 문항 재라벨링(100점 규칙)이 정확히 안 맞는 시험 5건 — `공법_모의_2024_1차`, `공법_모의_2014_1차`, `공법_모의_2013_3차`, `형사법_모의_2018_1차`, `형사법_모의_2017_3차`. 원래 그룹 배점 합이 100의 배수와 깔끔하게 안 맞아떨어짐(그룹 키가 "헌법행정","행정법-" 같은 이상한 값인 것으로 보아 애초에 `group_key()` 그룹핑 자체가 불안정했던 시험들로 추정) — 근사치(100점 이상에서 강제로 닫음)로 처리되어 페이지 탭 라벨이 실제와 다를 수 있음
17. 지문 안 사례 라벨(`〈제1문의2〉`)이 표시되는 시험이 공법 11/55·형사법 22/55뿐임 — 나머지는 문항 그룹 하나가 실제로는 사례 하나 속 세부 설문 그룹일 뿐이라(예: 문1·문2·문3·문4가 사실은 사례 하나의 네 소문항) 그룹 수와 지문상 사례 경계 수가 안 맞아 라벨을 안전하게 생략함. 페이지 탭 라벨(100점 규칙 기반)은 이 경우에도 항상 정확함
18. ~~민사법 변시 7건(1,2,3,5,9,12,14회)은 채점 근거 없음~~ — 2026-08-11 다시 재어 보니 **틀린 진단이었다.**
    - 5·9·12·14회는 그 사이 재OCR로 보강돼 지금은 건당 1.5k~4.9k자로 충분하다. **실제로 요약 수준인 건 1·2·3회 세 건뿐**이다(건당 400~1,600자, 문항 수보다 답안 건수가 적다).
    - **진짜 문제는 따로 있었다.** 워커가 모범답안을 `a.text`로 읽는데 데이터의 본문 필드는 `answerText`다. 그래서 채점기준표가 없는 **494건**(변시 30 + 창작문제 464)에서 모범답안이 통째로 `[object Object]`로 들어가고 있었다. 즉 "근거가 없다"가 아니라 **있는 근거를 안 쓰고 있었다.** 고친 뒤 변시 1회로 확인하니 제2문의1의 실제 쟁점(채권양도의 동일성·이의 유보 없는 승낙·상계항변)을 배점까지 나눠 채점한다.
    - 남은 보강거리: `casebook_cases.json`의 「민민소 사례 CBT실전답안」이 변시 1~15회 전부에 회차당 1.3만~1.5만 자로 붙어 있는데 `casebookAnswers`에는 안 들어가 있다. 1·2·3회를 메우려면 여기서 가져오면 된다.
19. ~~민사법 문항 재라벨링(100점 규칙)은 실제 PDF로 검증되지 않은 근사치~~ — 2026-07-27 **검증 완료, 규칙이 틀렸음이 확인됨**. 사용자가 제공한 제15회 민사법 실제 PDF 대조 결과 문당 배점은 **제1문 150 / 제2문 100 / 제3문 100**(총 350)이고 문항은 3개였다. 100점 규칙은 제1문~제4문(100/105/115/30)으로 잘라 문항 수도 배점도 틀렸다. → `relabel_civil.py`를 새로 만들어 원문 표제·채점기준표·PDF 대조를 근거로 19건을 확정하고, 근거가 없는 36건은 `groupSource:"estimated"`로 표시(아래 22번)
20. ~~민사법 쟁점 480개 중 상법 분류가 27개뿐(5.6%)~~ — 2026-08-11 **48개로 교정**. 짐작이 맞았다. 명의개서·대표소송·신주인수권·전환사채발행·자기주식취득·이사해임·어음항변처럼 명백한 상법 쟁점이 죄다 "민법"에 들어가 있었다. 민법→상법 22건, 민법→민사소송법 10건, 그리고 「관습법상법정지상권」이 "상법"으로 잘못 들어가 있던 것 1건(쟁점명 안의 **'상 법'정지상권**이 부분 문자열로 걸린 것)을 민법으로 돌렸다. 33건 전부 눈으로 확인했고 오분류는 없다
21. `_보류항목_사례집대조필요.md`(미해결 이슈 1번)의 민사법 2013년 3차 건 — 이번에 확보한 정연석 사례집 데이터로 대조 가능해졌을 수 있으나, 이번 작업에서는 시도하지 않음(범위 밖)
22. ~~민사법 55건 중 36건은 문항 라벨 근거가 없음(`groupSource:"estimated"`)~~ — 2026-08-11 **55건 전부 확정**. PDF를 따로 받을 필요가 없었다. 원본 시험지 hwp가 이미 있었고, 표제가 안 보였던 건 `hwp5txt`가 표(table)를 `<표>`로 치환하기 때문이었다. 시험지는 문항 표제를 하나같이 표 안에 넣는다. `hwp5proc xml`로 바꾸니 표제가 전부 살아났다 → `extract_exam_plans.py` + `relabel_from_paper.py` 신설, `relabel_civil.py` 삭제. 자동 추출 결과가 15회 손대조 결과(순서 재배열 + 문항별 설문 개수)와 완전히 일치해 방법을 검증했다. `groupSource`는 이제 55건 모두 `"시험지"`
23. ~~민사법 태깅 데이터 12건에서 `questions` 배열 순서·개수가 문제 원문과 어긋남~~ — 2026-08-11 **시험지 기준으로 전부 교정**. 어긋남은 세 종류였다. ① 순서가 뒤바뀐 4건(15회·2017\_2차·2020\_1차·2024\_3차) → 시험지 순서로 재배열. ② 앱이 설문 여럿을 한 덩어리로 묶어 둔 자리 → 시험지 쪽으로 쪼개고 `ask`는 시험지 원문으로 교체. ③ 앱 배점이 아예 틀린 2건(2023\_3차는 380점, 2024\_1차는 335점 — 둘 다 실제로는 350점) → 시험지를 진실로 삼아 재구성. 결과적으로 55건 전부 350점 만점이 맞는다. **다만 근본 원인인 태깅 데이터(`pipeline/source/민사법_사례_final.json`)는 그대로**이므로, 거기서 다시 빌드하면 같은 어긋남이 재현된다 — 반드시 `relabel_from_paper.py`를 뒤에 돌려야 한다
24. ~~형사법·공법 데이터에는 `groupSource` 필드가 없음~~ — 2026-08-11 **두 과목도 시험지에서 확정**. 세 과목 165건 모두 `groupSource:"시험지"`다. 공법·형사법은 100점 규칙이 실제 PDF로 검증됐으니 대체로 맞지만, 미해결 이슈 16번의 5건(근사 처리)은 사실상 추정이므로 나중에 `groupSource`를 붙여 구분하는 편이 정확하다

---

## 다음에 할 일

1. ~~사례집 목차 구조를 추출하고 "쟁점별로 한 문제씩 풀기" 모드를 그 형식에 맞춰 재설계~~ — 2026-07-27 완료 (위 참고)
2. ~~형사법을 공법과 동일한 파이프라인으로 앱에 반영~~ — 2026-07-27 완료 (위 참고)
3. ~~민사법을 공법·형사법과 동일한 파이프라인으로 앱에 반영~~ — 2026-07-27 완료 (위 참고). **다음은 국제법·국제거래법 차례**
4. 나머지 2과목(국제법/국제거래법)도 동일 파이프라인으로 완성 — 이 둘은 선택과목이라 15회 변시 데이터가 없고(변시에서 국제법/국제거래법은 선택과목 응시자만 봄, 우리 데이터는 40건 모의고사 위주일 가능성 높음), 공법·형사법·민사법과는 데이터 구성이 다를 수 있으니 시작 전에 `_태깅데이터_백업/국제법_사례_final.json`·`국제거래법_사례_final.json` 구조부터 확인할 것
5. ~~미해결 이슈 4·5번의 임시 스크래치패드 데이터(태깅 결과, 경향분석)를 영구 위치로 이전~~ — 2026-07-27 완료. 저장소 `pipeline/`(스크립트·소스·사례집블록·레지스트리) + `docs/경향분석_공법.md`로 이전, 재현성 검증까지 마침
6. ~~허브 저장소(`evenoa9218-gif.github.io`) 실제 대시보드·공용 서비스워커 구축~~ — 2026-07-27 **구축 완료, 로컬 커밋까지 됨(푸시 전)**. `index.html`(앱 진입 카드 + 과목별 학습량 집계, 바닐라 JS), `core/store.js`(CASE_Practice 정본 복사), `sw.js`(셸만 precache·`data/*.json`은 런타임 network-first), `manifest.json`+`icon.svg`, `README.md`. 브라우저 검증(서비스워커 등록·오프라인 폴백·테마 토글·모바일 1열·콘솔 에러 0건) 완료. **배포까지 끝남 — https://evenoa9218-gif.github.io/ 에서 동작 확인**(로그인 화면·앱 선택·세 위성앱 링크 200·서비스워커 등록·Pretendard 적용). 배포 시 `.nojekyll`이 필요했다 — 없으면 GitHub Pages가 Jekyll을 태워 저장소에 지정된 테마로 README를 대신 렌더링한다
7. `Core_Notes` 절대경로 버그 점검(있다면 `MCQ` 때와 같은 방식으로 경로만 정밀 수정)
7-1. **`MCQ` 저장소의 형사법 데이터 구조 불일치 정리** — 2026-07-27 조사 완료(아래 "MCQ 과목 간 데이터 구조" 참고). `_DATA` 배열→객체 변환 누락은 커밋 `9924f56`으로 이미 고쳐졌으나, ①형사법 문항 id가 민법과 달리 `"001"` 순수 일련번호라 기출연도 배지가 아예 렌더링되지 않고(`criminal.html:4486`의 정규식 `/^(\d+)\.(.+)$/`에 매칭 실패), ②`criminal.html:4497`의 연도 조회가 아직 민법 모듈명(`채권각론`·`민법총칙` 등)을 하드코딩하고 있으며, ③`EXAM_YEAR_형법총론/각론/형사소송법`이 선언만 되고 참조처가 0건인 데드 코드다. ④`SUBJECT_NS='criminal'` 네임스페이스가 통계에만 적용되고 계정·피드백·수정본은 루트 경로에 그대로 쓴다
8. `_보류항목_사례집대조필요.md` 2건 대조 확정 — 민사법 2013년 3차 건은 이번에 확보한 정연석 사례집으로 대조 가능할 수 있음(미해결 이슈 21번)
9. ~~AI 채점 기능(Cloudflare Worker 프록시) 설계/구현~~ — 2026-07-29 완료
10. `case-practice`(소문자) 폴더 삭제 여부를 사용자에게 확인 후 정리
11. (낮은 우선순위) 공법 사례집 목차 매칭에서 문항 그룹 정밀도가 어긋난 소수 사례 수동 보정
12. (낮은 우선순위) 형사법 변시 1~5회 채점근거 공백을 이인규 형법 변사기 책으로 형법 파트만이라도 채우는 방안 검토
13. ~~형사법·민사법 사례집 목차 모드는 필요해지면 별도 설계 검토~~ — 2026-08-05 둘 다 완료 (미해결 이슈 13번 참고)
14. 문항 재라벨링 100점 규칙이 안 맞는 5개 시험(미해결 이슈 16번) 수동 보정
15. 지문 내 사례 라벨이 안 보이는 나머지 시험(공법 44·형사법 33·민사법 55 = 132/165)도 표시되게 하려면, 텍스트 사례 경계 탐지를 더 정교하게 하거나 애초에 태깅 단계에서 `questionStructure`를 사례 단위로 정확히 구조화하는 방법 검토 필요 — 지금은 안전하게 라벨을 생략하는 선에서 마무리함
16. ~~민사법 문항 재라벨링(100점 규칙)을 실제 PDF로 검증~~ — 2026-07-27 완료(미해결 이슈 19번). **후속 완료**: 2026-08-11 원본 시험지 hwp에서 표제를 직접 읽어 55건 전부 확정(미해결 이슈 22번)
17. 민사법 상법 쟁점 분류 키워드 보강(미해결 이슈 20번) — 현재 27개뿐으로 너무 적음

---

## 디자인 통일 (2026-07-27)

세 앱(허브·MCQ·CASE_Practice)의 활자와 색을 맞췄다.

- **폰트**: 전 페이지 Pretendard. MCQ는 `font-family`에 이름만 적혀 있고 웹폰트를 받지 않아 사실상 시스템 폰트로 폴백되던 상태였다 — dynamic-subset CSS를 import하고 `'Pretendard Variable'`(실제 등록명)을 앞에 두도록 고쳤다. MCQ 커밋 `6bbf163`
- **색**: 라이트 핵심 토큰 12개(`--bg:#ecf4fb` 등)는 원래 이미 같았고, 다르던 `--ok`/`--no`를 MCQ 값(`#16a34a`/`#dc2626`, 다크 `#4ade80`/`#f87171`)으로 통일
- **기본 테마**: 세 앱 모두 라이트가 기본, 다크는 토글 (저장 키만 다름 — `quiz_theme`/`cp_theme`/`lawhub_theme`)
- **허브 로그인·앱선택 화면**: MCQ 로그인 화면과 같은 글래스모피즘(어두운 그라디언트 배경 + 투명 카드)으로 맞춤. 대시보드는 라이트 카드 UI 유지

**남은 것**: MCQ 로그인 배경이 28MB 영상 제거 후 CSS 그라디언트인데 색감이 아쉽다는 지적이 있었다. 배경 사진으로 바꾸려면 WebP/AVIF로 최적화하면 200KB 안팎이라 로딩 부담은 사실상 없다(영상의 1/100 이하) — 사진 파일을 받으면 적용하면 된다.

---

## MCQ 과목 간 데이터 구조 (2026-07-27 조사)

"형사법이 민법과 다른 구조로 쌓였다"는 지적이 사실인지 `MCQ` 저장소를 실제로 조사한 결과다.
**문항 컨테이너 스키마는 통일됐지만, id 형식과 기출연도 처리는 아직 다르다.**

| 항목 | 민법(`index.html`) | 형사법(`criminal.html`) | 상태 |
|---|---|---|---|
| `_DATA` 변환 | `[id,q,a,e,t]` → 객체 | 동일 | ✅ `9924f56`으로 통일됨 |
| 문항 `id` | `"001.①"` (문항번호.선지) | `"001"` (순수 일련번호) | ❌ 마이그레이션 미적용 |
| 기출연도 배지 | `EXAM_YEAR` 맵으로 표시 | **표시 안 됨** (정규식 불일치) | ❌ |
| 연도 조회 코드 | 민법 모듈명 분기 | 민법 모듈명 그대로 복붙 | ❌ `criminal.html:4497` |
| 연도 상수 | 사용 중 | `EXAM_YEAR_형법총론` 등 참조 0건 | ❌ 데드 코드 |
| 기출출처 | `EXAM_YEAR` 맵에 분리 | 문제문에 `[변25]` 인라인 388회 | ⚠️ 방식 자체가 다름 |
| Firebase 경로 | `users/…` | `criminal/users/…` (통계만) | ⚠️ 의도된 분리이나 불완전 |
| `starWhite` 필드 | 없음 | 형법총론에만 있음 | ⚠️ 비대칭 |

- `localStorage` 키(`qhub_` 접두사)는 세 파일 모두 동일하다.
- `public-law.html`은 문항이 0건인 껍데기인데 민법 `EXAM_YEAR`/`SS_EXAM` 데이터가 복붙된 채 남아 있다.
- `questions.js`/`questions.json`은 정수 id를 쓰는 3세대 스키마로, 어디서도 참조되지 않는 고아 파일이다.

이 저장소는 `CASE_Practice`와 별개이므로 수정은 별도 작업으로 잡아야 한다(다음에 할 일 7-1번).

---

## 시도했다가 버린 접근과 그 이유

1. **zip 파일 복원에 `zip`/`unzip` CLI 사용** → `zip` 명령이 시스템에 없어 실패 → Python `zipfile` 모듈로 재압축하는 방식으로 대체(성공)
2. **hwp 파싱에 한글(HWP) 프로그램 COM 자동화 사용** → 사용자 메모리(`hwp-pdf-conversion.md`)에 기록된 재부팅 직후 실행 금지·보안 경고창·좀비 프로세스 리스크 때문에, 960개 이상 파일 규모에는 비현실적이라 배제 → 순수 파이썬 `pyhwp` 라이브러리로 대체
3. **파일명의 `제N문` 텍스트 마커로 사례형 문항 페이지 분리** → 55건 중 1건에만 마커가 존재해 일반화 불가 → 이미 구조화되어 있던 `questionStructure` 필드 기반 그룹핑으로 대체
4. **hwp 전체 텍스트에서 `【참조조문】` 같은 블록 경계로 지문을 잘라 문항 나누기** → 회차마다 포맷이 달라 불안정 → 위와 동일하게 `questionStructure` 그룹핑으로 대체
5. **사례집 PDF 폴더 전체를 즉시 자동 배치변환** → 사용자가 "임의로 접근하지 말고, 내가 넣어주면 그때 진행하라"고 제지 → 즉시 중단하고 이미 생성된 파일까지 롤백
6. **정제 규칙에 조사(은/는/이/가) 앞 공백 자동 병합 추가** → "있는 이 사건"의 "이"가 조사가 아니라 관형사인 사례에서 "있는이 사건"으로 원문이 훼손됨을 발견 → 한국어로는 기계적 구분이 불가능하여 규칙 완전 제거
7. **저장소 개명(`3000`→`MCQ`) 반영 시 문자열 전체 치환** → 판례번호 `2003도3000`이 `2003도MCQ`로 손상될 위험을 사전에 발견 → 경로(`/3000/`)만 정밀하게 치환하는 방식으로 대체
8. **`제15회변호사시험정답가안.zip`을 "이미 분류 폴더에 동일 파일 있음"이라 판단해 사전 확인 없이 삭제** → 사용자가 "그건 그대로 뒀어야 하는데"라고 지적 → 동일 내용 파일로 재압축해 원래 경로·이름으로 즉시 복구. **이후로는 삭제 전 반드시 확인받기로 함**
9. pyhwp 설치가 여러 차례 응답 지연(백신 프로그램 스캔으로 추정)을 겪었으나, 재부팅 등 강한 조치 없이 대기만으로 결국 정상 완료됨 — 좀비 프로세스 상태는 아니었음

---

## 수정한 파일 목록

### `CASE_Practice` 저장소 (github.com/evenoa9218-gif/CASE_Practice)
- `index.html` — 앱 본체 (여러 차례 재작성: 초기 구축 → 라이트테마/쟁점별풀이/문항페이지분리 → 사례집 목차 브라우저 → 지문/문제/조문 서식 분리 → 다과목 지원(SUBJECT 하드코딩 제거, 과목 전환 UI) → 좌우 2단 레이아웃 → 답안 줄 표시)
- `core/store.js` — 4체계 공용 저장 추상화 레이어 (신규)
- `data/issues_공법.json` — 쟁점 레지스트리 (신규)
- `data/issues_형사법.json` — 쟁점 레지스트리, CRI 접두사 550개
- `data/issues_민사법.json` — 쟁점 레지스트리, CIV 접두사 480개 (신규, 2026-07-27)
- `data/공법/index.json`, `data/공법/exams/*.json`(55개) — 회차별 상세 데이터
- `data/공법/casebook_toc.json` — 사례집 목차(편>사례) + 시험/문항 매칭 데이터
- `data/형사법/index.json`, `data/형사법/exams/*.json`(55개, 3.5MB) — 회차별 상세 데이터. ~~`casebook_toc.json`은 없음~~ → 2026-08-05 추가됨(3권)
- `data/민사법/index.json`(451KB), `data/민사법/exams/*.json`(55개, 6.3MB) — 회차별 상세 데이터 (신규, 2026-07-27). ~~`casebook_toc.json`은 없음~~ → 2026-08-05 추가됨(4권). **2026-08-07 로사정 163건이 더해져 218개 / 779KB / 7MB**
- `README.md` — 갱신 (사례집 목차 모드·문제 화면 서식·형사법/민사법 반영 내용 추가)
- `HANDOFF.md` — 이 문서
- 2026-07-27 추가 커밋: `0707ae4`(민사법 문항 라벨 재부여 + `relabel_civil.py` 신규) → `724b9c8`(Pretendard 통일·색 토큰 정렬)
- 2026-07-28 ~ 08-07 커밋 이력(위 "2026-08-08 갱신" 참고): `a5024e6`(기록형 앱 배포 반영) → `fc77ad5`(**AI 채점 켜기** — Worker 배포 주소 연결) → `664fa87`(헤더 허브 버튼) → `94209cd`(민사법 모범답안 25→53회차) → `a23b12a`·`541c883`(형사법 사례집 이창현 449 + 이인규) → `256491e`·`db260b2`(민사법 송영곤 65 + 인사이트 상법 153) → `3205909`(박승수 CBT 30) → `af85cbf`(형사법 모범답안 10→15회차, 깨짐 51%→2%) → `8853239`(이창현 판본 교체 + 작은변사기 103) → `7e46d60`(공법 정선균 12) → `291d9f7`(정연석·이인규 **재OCR**, 깨짐 55%→1%) → `a38f57a`(목차 숫자순 정렬) → `9c6fb9c`·`ea9a3c5`(인사이트·송영곤 재OCR, 24%→9%→0%) → **`4c7f051`(로사정 163 창작문제 추가, 55→218건)** → `cea6045`(로사정 본문 정리) → `06a26cb`(책 오기 교정 + `_config.yml`) → `3756c06`(Pages 재시도) → `84ef89f`(**`?exam=` 딥링크**) → `e74e79c`(로민정↔로사정 대응표)
- 커밋 이력: `0d145eb`(초기 구축) → `51af271`(텍스트 정제) → `60d3795`(라이트 테마·쟁점별 풀이·문항 페이지 분리) → `cb7c584`(사례집 목차 브라우저) → `248322a`(HANDOFF 해시 수정) → `9e417e6`(지문/문제/조문 서식 분리) → `bbe03d8`(형사법 전체 반영 + 다과목 지원) → `7152cde`(HANDOFF 해시 수정) → `eb94f25`(문항 라벨을 실제 시험 표기로 재부여) → `14169ea`(HANDOFF 해시 수정) → `926d284`(문제 풀이 화면 좌우 2단 레이아웃) → `484639e`(HANDOFF 해시 수정) → `a7b0160`(답안 줄 표시·버튼 라벨 통일) → `bd4fc2d`(민사법 전체 반영: 사례집 매칭·쟁점 레지스트리·다과목 지원)

### 파이프라인 스크립트·소스데이터 — 2026-07-27 저장소로 이전 완료
**이전에는 임시 스크래치패드에만 있어 유실 위험이 있었으나, 전부 저장소 안 `pipeline/`으로 옮겼다.**
다른 계정·다른 PC에서도 클론만 하면 `data/` 전체를 재생성할 수 있다.

- `pipeline/scripts/` — 스크립트 57개(2026-08-07 기준 **77개**). 하드코딩된 절대경로는 전부 `paths.py` 상수 참조로 치환
  - `paths.py`(신규) — 저장소 안 경로는 `__file__` 기준 상대경로, 저장소 밖 개인자료(원본 hwp/pdf, hwp5txt)는 환경변수(`CASE_RAW`/`CASE_PDF_ROOT`/`CASE_WORK`/`HWP5TXT`)로 지정
  - 과목별 4단 체인: `merge_casebook_{과목}.py` → `clean_apply_{과목}.py`(공법은 `apply_clean.py`) → `build_issue_registry{_과목}.py` → `build_case_app_data{_과목}.py`, 이후 `relabel_groups.py`
  - `split_casebook{,_criminal,_civil}.py` — 책별 사례집 파싱(마커 형식이 책마다 달라 공용 파서 불가)
  - `parse_toc.py`·`match_toc.py`·`build_data_file.py` — 공법 사례집 목차 파싱/매칭/생성
- `pipeline/source/` — 태깅 결과 정본 입력. `{과목}_사례_final.json` 5과목 + `공법_변시_final.json`
- `pipeline/casebook/` — `casebook_blocks_공법.json`, `criminal_casebook_blocks.json`(20블록), `civil_casebook_blocks.json`(566블록), `toc_raw.json`, `toc_matched.json`
- `pipeline/registry/` — `issues_*.json`, `exam_issues_*.json` 스냅샷
- `pipeline/work/` — 중간 산출물, `.gitignore`로 제외(재생성 가능)
- `pipeline/README.md`(신규) — 단계별 스크립트 역할, 재생성 명령, 책별 마커 형식, 알려진 한계
- `docs/ARCHITECTURE.md`(신규) — 허브+위성 통합 시스템 설계(오리진 공유 이유, store.js API 계약, 쟁점 ID 체계, 프라이버시 경계, 100점 규칙, AI 채점 계획)
- `docs/경향분석_공법.md` — 백업 폴더에만 있던 공법 출제경향 분석을 저장소로 이전

**재현성 검증**: 빈 작업폴더에서 `pipeline/source`+`pipeline/casebook`만으로 공법·형사법·민사법 3과목
체인을 처음부터 다시 돌린 결과가 커밋된 `data/`와 **바이트 단위로 동일**함을 확인했다(`git diff` 무변화).
이 과정에서 공법 하류 스크립트 3개가 정제 **전** 파일(`공법_사례_full.json`)을 읽던 버그를 발견해
정제본(`_full_clean.json`)을 읽도록 고쳤다 — 옛 세션에서 `_full.json`을 정제 결과로 덮어써서
드러나지 않던 문제였다.

### `3000`(`MCQ`) 저장소 (github.com/evenoa9218-gif/MCQ, 로컬 경로 `projects/3000`)
- `index.html`, `criminal.html`, `public-law.html`, `manifest.json`, `sw.js` — 저장소 개명(`3000`→`MCQ`)으로 깨진 절대경로 수정
- 배경영상(28MB, 로그인/과목선택 화면) 제거 → CSS 그라디언트로 대체
- 커밋 이력: `545fff8`(경로 버그 수정) → `13055d3`(배경영상 제거)

### 로컬 원본 데이터 (`Desktop/2026/변시대비 자료 및 pdf/모의고사 기출문제, 모범답안/`)
- 6개 과목 폴더 구조 전면 재편(문제/채점기준표 → 선택형/사례형/기록형 하위분류), 약 1,900개 이상 파일 이동, 700개 이상 삭제(선택과목 외 파일 + 완전중복)
- 각 hwp 파일 대응 `.txt` 추출본 생성(과목별 `txt` 하위폴더에 정리)
- 사례집 PDF → txt 변환(헌법 2권 · 형법 · 형사소송법 · 행정법 · 민사소송법 · 민법 순으로 진행, 민법은 마지막 권 진행 중 세션 종료)
- `_보류항목_사례집대조필요.md` 신규 생성

### 참고만 하고 수정하지 않은 파일
- `C:\Users\82109\.claude\projects\C--Users-82109\memory\hwp-pdf-conversion.md` (조회만 함)

### 백업 (2026-07-27) — 이후 저장소로 정식 이전됨
경로: `Desktop/2026/변시대비 자료 및 pdf/모의고사 기출문제, 모범답안/_태깅데이터_백업/`
`{과목}_사례_final.json` 4과목, `{형사법,민사법}_사례_full_clean.json`, `issues_{형사법,민사법}.json`,
`{criminal,civil}_casebook_blocks.json`, `공법_경향분석.md`.

이 폴더는 임시폴더 유실에 대비한 1차 안전망이었고, **지금은 저장소 `pipeline/`·`docs/`가 정본**이다
(`_사례_final.json` → `pipeline/source/`, 사례집 블록 → `pipeline/casebook/`,
레지스트리 → `pipeline/registry/`, 경향분석 → `docs/경향분석_공법.md`).
백업 폴더는 그대로 두되, 앞으로 갱신할 필요는 없다. `_full_clean.json` 같은 중간 산출물은
`pipeline/scripts`로 언제든 재생성되므로 커밋하지 않았다.

국제법·국제거래법은 `pipeline/source/`에 태깅 결과만 있고 사례집 매칭·정제·앱 반영은 아직 전이다.

---

## 2026-08-11 — AI 채점 403 "Request not allowed"의 진짜 원인

크레딧도 키도 모델도 아니었다. **Worker가 실행된 지역이 문제였다.**

진단 순서가 중요하다. 최소 토큰으로 하나씩 좁혔다.

1. 모델을 opus-5 / sonnet-5 / haiku-4.5로 바꿔 봐도 **전부 403** → 모델 권한 문제 아님
2. `models.list()`도 403 → 특정 엔드포인트 문제 아님
3. **일부러 틀린 키로 불러도 403 forbidden** (정상이라면 401 authentication_error가 나와야 한다)
   → **키를 보기도 전에 차단**되고 있다는 뜻. 키·크레딧 문제가 아님이 여기서 확정된다
4. 원시 `fetch`로 응답 헤더를 보니 `cf-ray: …-HKG` → **홍콩 PoP**에서 나갔다.
   Anthropic은 미지원 지역의 요청을 인증 전에 끊는다

Cloudflare Workers는 요청을 가까운 PoP에서 실행하는데, 한국에서 부른 요청이 홍콩으로
가는 일이 있다. 그래서 증상이 **간헐적**이다 — 어제 되던 것이 오늘 안 되고, 재시도하면
갑자기 된다. 이것 때문에 지난번에는 "일시적 장애"로 넘겨 짚었다.

조치: `wrangler.toml`에 `[placement] mode = "smart"`를 넣어 Worker를 백엔드 가까이
배치하게 했고, 403일 때 오류 메시지에 실행 PoP를 함께 보여주도록 했다
(`request.cf.colo`). **다음에 또 403이 뜨면 크레딧을 의심하기 전에 그 지역 코드부터 볼 것.**

여전히 막히면 남은 선택지는 AI Gateway 경유 또는 프록시를 다른 호스팅으로 옮기는 것이다.

---

## 2026-08-11 (이어서) — 공법·형사법도 시험지 기준으로 확정

민사법에서 통한 방법을 두 과목에 넓혔다. 결과는 **165건 전부 확정**(`groupSource:"시험지"`),
총점은 공법·형사법 200점 / 민사법 350점으로 전부 만점과 맞는다.

### 공법·형사법은 시험지에 하위 표제가 없다

시험지를 열어 보면 `〈제1문〉`·`〈제2문〉` 둘뿐이다(공법은 최근 9건만 `제1문의1`이 실재).
**앱에 있던 `제1문의1`·`설문1..11`·`사례1-`은 시험지에 없는 표기였다** — 100점 규칙으로
배점을 잘라 만든 것이다.

그렇다고 시험지 그대로 두 칸으로 만들면 한 칸에 100점 분량을 써야 한다. 그래서 문항
표기는 시험지를 따르되 설문 번호를 붙여 `제1문-1`·`제1문-2`로 나눴다. 답안 칸이
공법 211→504, 형사법 236→582로 늘었고 설문 하나씩 채점받을 수 있다.

### 앞선 보고 정정 — 민사법 2건이 틀려 있었다

"민사법 55건 전부 확정"이라고 했는데 2013년 1·2차 두 건이 **한 문항으로 뭉쳐 있었다**.
이 두 회차만 표제가 `〈 제 1-2 문 〉`처럼 붙임표를 쓴다. 정규식이 `제N문의M`만 보고 있어
본문 표제를 통째로 놓쳤고, 그러자 답안지 유의사항에 적힌 `제3문`이 기준으로 남아
뒤따르는 진짜 표제가 전부 걸러졌다(표제는 오름차순이라는 규칙에 걸린 것이다).

`제1-2문` 표기를 받도록 고쳤고, **추출 뒤 "제1문으로 시작하지 않는 시험"을 경고로 찍게
했다.** 같은 사고가 나면 바로 드러난다. 지금은 165건 모두 제1문에서 시작한다.

### 배점 검산은 만점으로 한다

공법 11건·형사법 1건이 만점을 넘었다. 시험지가 「1. …답하시오. (45점)」 아래
「① …(10점) ② …(5점)」처럼 **문항 총배점을 같이 적기** 때문이다 — 그대로 더하면
45를 두 번 세게 된다. 뒤따르는 배점 둘 이상의 합과 같은 자리를 소계 후보로 보고,
빼서 만점이 되는 조합이 **하나뿐일 때만** 뺀다. 여럿이면 그 시험은 보류한다.
